.venv/
.env
geotab_sync.log
# Rotina de atualização local (logs e marcador de "rodou hoje")
atualizacao_local.log
.ultima_atualizacao
# Logs de execuções avulsas (backfills, geocode)
*_run.log
__pycache__/
# CSVs gerados p/ download externo (recriados a cada export)
exports/
# CSVs de diagnostico/levantamento (saidas de consulta, nao fonte).
# Pasta PROPRIA, e nao dentro de exports/: o exportar_csv.py esvazia a
# exports/ a cada execucao (unlink em tudo) e apagaria estes arquivos --
# e uma subpasta la dentro quebraria o export, pois unlink falha em diretorio.
diagnosticos/
